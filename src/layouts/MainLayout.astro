---
interface Props {
  title: string;
}
const { title } = Astro.props;
import '../styles/global.css';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>

  <body class="flex flex-col min-h-screen w-full">

    <header
      class="relative overflow-hidden bg-gradient-to-r from-maya-dark via-maya-deep to-maya-dark bg-cover bg-no-repeat"
      style="background-image: url('/dragones6.png'); background-position: 100% 20%; background-size: cover;"
    >
      <div class="absolute inset-0 bg-black/30 pointer-events-none"></div>
      <div
        class="relative z-10 container mx-auto px-6 py-8 grid grid-cols-1 md:grid-cols-2 gap-8 items-center min-h-[250px] md:min-h-[300px] lg:min-h-[350px]"
      >
        <div class="hidden md:block"></div>

        <div class="text-center md:text-right md:pr-12">
          <h1 class="text-3xl md:text-4xl font-bold text-white mb-2 leading-tight">
            Bienvenidos a
          </h1>
          <h2 class="text-3xl md:text-4xl font-bold text-white mb-3 leading-tight">
            KAIloop Nexus
          </h2>
          <p class="text-xl text-green-500 italic font-bold leading-snug">
            El centro de gestión
          </p>
          <p class="text-xl text-green-500 italic font-bold leading-snug">
            Inteligente de Kukulcan AI
          </p>
        </div>
      </div>
    </header>

    <main class="flex-grow w-full">
        <slot />
    </main>

		<footer class="bg-gradient-to-r from-[#0f4c65] to-[#093145] text-white py-10 px-4 md:px-8 relative overflow-hidden w-full mt-auto border-t border-[#1a6b8a]">

  <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between z-10 relative w-full">

    {/* Sección de Texto */}
    <div class="w-full md:w-2/3 text-center md:text-left mb-8 md:mb-0 md:pr-10">
      <p class="text-lg leading-relaxed font-light text-slate-100">
        La neurona <span class="font-bold text-cyan-300">VCS</span> impulsa una nueva era de monetización B2B,
        transformando datos de comportamiento en decisiones rentables
        que maximizan ingresos, eficiencia y fidelización.
      </p>
    </div>

    {/* Sección de Logo */}
    <div class="w-full md:w-1/3 flex flex-col items-center md:items-end">
      <div class="flex items-center gap-3 group cursor-default">
        {/* SVG optimizado para fondo oscuro */}
        <svg width="60" height="40" viewBox="0 0 200 100" fill="none" xmlns="http://www.w3.org/2000/svg"
             aria-labelledby="logoTitle logoDesc" role="img" class="flex-shrink-0 drop-shadow-md group-hover:scale-105 transition-transform duration-300">
          <title id="logoTitle">Kukulcan AI Consulting Logo</title>
          <desc id="logoDesc">Logo de Kukulcan AI Consulting</desc>

          {/* Forma del escudo/dragón simplificada */}
          <path d="M20 50 C 30 20, 70 20, 80 50 C 70 80, 30 80, 20 50 Z" fill="white" fill-opacity="0.9"/>
          <circle cx="25" cy="45" r="4" fill="#0f4c65"/>
          <path d="M22 47 Q25 42, 28 47" stroke="#0f4c65" stroke-width="2" fill="none"/>

          {/* Texto SVG */}
          <text x="90" y="45" font-family="Arial, sans-serif" font-size="28" font-weight="bold" fill="white">Kukulcan AI</text>
          <text x="90" y="70" font-family="Arial, sans-serif" font-size="16" fill="#cbd5e1" letter-spacing="1">Consulting</text>
          <line x1="90" y1="78" x2="190" y2="78" stroke="#16a3b6" stroke-width="2"/>
        </svg>
      </div>
    </div>

  </div>

  {/* Elementos Decorativos de Fondo (Marcas de agua sutiles) */}
  <div class="absolute inset-0 z-0 pointer-events-none">
    <div class="absolute top-[-10%] left-[-5%] w-32 h-32 bg-white opacity-[0.03] rounded-full blur-2xl"></div>
    <div class="absolute bottom-[-10%] right-[-5%] w-40 h-40 bg-cyan-400 opacity-[0.05] rounded-full blur-3xl"></div>

    <div class="absolute top-1/4 left-1/4 w-8 h-8 bg-white opacity-5 transform rotate-45"></div>
    <div class="absolute bottom-1/3 right-1/4 w-6 h-6 bg-white opacity-5 transform rotate-12"></div>
    <div class="absolute top-1/2 left-2/3 w-2 h-2 rounded-full bg-cyan-300 opacity-20"></div>
  </div>

</footer>


</body>
</html>
